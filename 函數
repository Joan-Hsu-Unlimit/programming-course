#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int f(int n);
int main(void)
{
	int n;
	do{
	printf("n:");
	scanf("%d",&n);
	}while(n<0);
	
	printf("1加到%d=%d",n,f(n));
	
	system ("pause");
	return 0;
}
int f(int n)
{
	int sum=0;
	for(int i=1;i<=n;i++)
		sum=sum+i;
	return sum;
}
